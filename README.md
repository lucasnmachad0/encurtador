# Encurtador de URL

Aplicação web em Python + Flask que encurta links longos.

## Como rodar

```bash
git clone https://github.com/lucasnmachad0/encurtador.git
cd encurtador
pip install -r requirements.txt
python app.py
```

Acesse http://localhost:5000